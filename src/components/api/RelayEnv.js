// import {Environment, Network, RecordSource, Store} from 'relay-runtime';
import fetchGQL from "../../api/fetchGQL";

// async function fetchRelay(params, vars) {
//   console.log(`fetching query with ${params.name} with ${JSON.stringify(vars)}`);
//   // TODO: return call to 'fetchGraphQL'
//   return fetchGQL(params.text, vars)
// }

// export default new Environment({
//   network: Network.create(fetchRelay),
//   store: new Store(new RecordSource())
// })
