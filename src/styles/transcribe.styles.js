import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  resultsWrapper: {
    marginTop: 10,
  },
  resultContainer: {
    paddingLeft: 10,
    paddingRight: 15,
    marginHorizontal: 10,
    marginBottom: 10,
  },
});

export default styles;
